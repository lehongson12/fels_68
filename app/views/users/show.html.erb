<% provide :title, @user.name %>
<div class="row">
  <aside class="col-md-4">
    <ul class="users">
      <%= render @user %>  
    </ul>
    <section class="stats">
      <%= render "shared/stats" %>
      <%= render "follow_form" if logged_in? %>
    </section>
  </aside>
</div>