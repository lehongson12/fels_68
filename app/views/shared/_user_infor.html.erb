  <% if current_user.avatar? %>
    <%= image_tag current_user.avatar.url, size: Settings.avatar.home_user_avatar_size %>
  <% else  %>
    <%=  gravatar_for current_user%>
  <% end %>
</span>
<h3><%= current_user.name %></h3>
<span><%= link_to t("view_profile"), current_user %></span>
